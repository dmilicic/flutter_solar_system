{
  "rules": {
    "spaceships": {
      "$spaceship_id": {
        ".read": "true",
        ".write": "true",
        ".validate": "newData.hasChildren(['id', 'name', 'locationX', 'locationY', 'lastUpdated'])"
      }
    }
  }
}